package model.component.base;

import org.openqa.selenium.WebDriver;
import runner.BaseModel;

public abstract class BaseSideMenuComponent extends BaseModel {
    public BaseSideMenuComponent(WebDriver driver) {
        super(driver);
    }
}